import {
  EMPLOYEE_UPDATE
} from '../actions/types';

const INITIALIZE_STATE = {};

export default (state = INITIALIZE_STATE, action) => {
  switch (action.type) {
    default:
      return state;
  }
};
